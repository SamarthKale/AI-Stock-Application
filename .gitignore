*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Firebase config — contains project-specific API keys; repo is public, never commit this.
/app/google-services.json

# Backend (Phase 3) — Gradle build output; the root /build rule above only matches the app module.
backend/build
backend/bin

# AI service (Phase 5) — trained model artifacts and raw training data are large/regenerable
# (re-run ai-service/training/fetch_training_data.py + train_xgboost.py to reproduce), not
# source; local Python cache and the dev-server log are pure local-run byproducts.
ai-service/artifacts/
ai-service/data/
ai-service/uvicorn.log
ai-service/__pycache__/
ai-service/**/__pycache__/
ai-service/.venv/
