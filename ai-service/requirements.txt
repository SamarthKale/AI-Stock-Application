fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
# Platform-split: xgboost's plain x86_64 wheel bundles GPU/CUDA device code (and pulls in
# nvidia-nccl-cu12) that this CPU-only inference service never uses — xgboost-cpu is the same
# project's official CPU-only x86_64 build (see docker/ai-service.Dockerfile's comment). It's
# only published for x86_64 (per its own PyPI description), so arm64 stays on the plain package,
# which is already a small, CPU-only build there (~13MB) with nothing to swap.
xgboost-cpu==2.1.3; platform_machine != 'aarch64'
xgboost==2.1.3; platform_machine == 'aarch64'
pandas==2.2.3
numpy==2.1.3
scikit-learn==1.5.2
joblib==1.4.2
requests==2.32.3
